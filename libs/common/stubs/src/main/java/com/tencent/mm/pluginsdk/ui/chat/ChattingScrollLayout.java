package com.tencent.mm.pluginsdk.ui.chat;

import android.content.Context;
import android.util.AttributeSet;
import android.view.ViewGroup;

/**
 * Stub for WeChat's ChattingScrollLayout (chat page scroll container). Compile-time only.
 */
public class ChattingScrollLayout extends ViewGroup {
    public ChattingScrollLayout(Context context) {
        super(context);
    }

    public ChattingScrollLayout(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    @Override
    protected void onLayout(boolean changed, int l, int t, int r, int b) {
    }
}
